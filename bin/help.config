#!/usr/bin/env bash

cat <<'EOS'
Prebuilt binaries only exist for macOS (x64/arm64) and Linux (x64).

Set GITHUB_API_TOKEN to avoid GitHub API rate limits when listing or
installing versions (e.g. in CI).
EOS
